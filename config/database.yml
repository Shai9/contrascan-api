default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS", 5) %>

development:
  <<: *default
  database: contrascan_development

test:
  <<: *default
  database: contrascan_test

production:
  url: <%= ENV["${{ Postgres.DATABASE_URL }}"] %>
